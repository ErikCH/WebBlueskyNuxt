{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^18",
    "@unocss/nuxt": "^0.51.12",
    "nuxt": "^3.4.3"
  },
  "dependencies": {
    "@atproto/api": "^0.2.11",
    "dotenv": "^16.0.3"
  }
}
